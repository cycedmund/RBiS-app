import { MrMiyagi } from "@uiball/loaders";

const Loading = () => {
  return (
    <div className="flex items-center justify-center min-h-screen">
      <MrMiyagi size={100} lineWeight={4.5} speed={2} color="#7299f2" />
    </div>
  );
};

export default Loading;
